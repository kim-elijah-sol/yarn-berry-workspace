{
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "jsx": "react", // react 컴포넌트를 사용하기 위해 jsx를 react로 설정
    "outDir": "./dist",
    "types": ["node", "jest", "@testing-library/jest-dom"]
  },
  "exclude": ["node_modules", "dist"]
}
